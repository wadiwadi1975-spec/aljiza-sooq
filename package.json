{
  "name": "aljiza-sooq",
  "version": "1.0.0",
  "description": "AL-JIZA SOOQ.COM — marketing bridge between businesses and the public",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check api/index.js && node --check api/db.js && node --check server.js && echo CHECK-OK"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "pg": "^8.13.0"
  }
}
